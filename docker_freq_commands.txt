# 启动服务
docker-compose up -d

# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 查看日志
docker-compose logs -f mongodb
docker-compose logs -f redis

# 进入容器
docker exec -it stock_mongodb bash
docker exec -it stock_redis sh

# 清理数据（谨慎使用）
docker-compose down -v